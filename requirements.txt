# Core ML / data
numpy>=2.2,<3.0
pandas>=2.3,<3.0
PyYAML>=6.0,<7.0
h5py>=3.10,<4.0

# Vision
opencv-python>=4.10,<5.0
ultralytics>=8.3,<9.0

# PyTorch
torch>=2.6,<2.7
torchvision>=0.21,<0.22

# Optional local verification
pytest>=9.0,<10.0
trackeval>=1.3,<2.0

# CARLA is usually installed separately because versioning is tied to the simulator build.
